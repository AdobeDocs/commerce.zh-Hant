name: Maintain includes metadata timestamps

on:
  schedule:
    - cron: '0 6 * * 1-5'  # 6:00 AM UTC, Monday-Friday
  workflow_dispatch:

concurrency:
  group: maintain-includes-metadata
  cancel-in-progress: false

jobs:
  maintain-metadata:
    uses: Adobe-Enterprise-Docs/commerce-operations.en/.github/workflows/maintain-includes-metadata.yml@main
    secrets: inherit
